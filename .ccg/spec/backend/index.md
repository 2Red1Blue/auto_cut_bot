# Backend mandatory specifications

Before changing Pipeline model input/output, prompts, response schemas, parsers,
compilers, runtime authority, or their acceptance tests, read
[VLM observation boundary](vlm-observation-boundary.md) in full.

These are mandatory design/review rules. Historical implementation behavior does
not grant an exception. The runtime enforcement checks listed in that specification
must be implemented before activating the replacement VLM contract.
