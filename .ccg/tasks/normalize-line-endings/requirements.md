# Requirements

- Repository text files must check out as LF on macOS, Linux and Windows.
- Windows command scripts may remain CRLF where explicitly declared.
- The existing desktop worktree's three untracked GPU deployment files must not
  be removed or overwritten.
- Only tracked changes proven to be line-ending-only may be reverted.
- Verify the policy in a clean worktree before committing it.
