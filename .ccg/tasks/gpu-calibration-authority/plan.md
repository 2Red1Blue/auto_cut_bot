# Plan

1. Inventory current CPU-only profile grammar, service profile verification,
   compose deployment, and authority installation consumers.
2. Specify a distinct GPU shadow profile version and validation rules that bind
   current service bytes, device identity, model trees, dependency versions,
   decoder/native identity, and policy hashes.
3. Implement GPU shadow-only parsing/projecting plus protected deployment
   assets; do not relax the normal timed-speech route or local-run resolver.
4. Complete the durable local measurement finalizer/exact reader and connect
   only the shadow measurement endpoint to it.
5. Run focused contract/negative tests and an independent review.
6. On the PC, rebuild from the current source, generate a fresh shadow profile,
   run calibration with independent anchors, validate/store the record, install
   the new local-run authority, then submit the authenticated HTTP Pipeline
   request from WSL.
