x-bwrap-security: &bwrap-security
  cap_add:
    - SYS_ADMIN
  security_opt:
    - apparmor=unconfined
    - seccomp=unconfined

services:
  auto_cut_bot-gateway:
    <<: *bwrap-security

  auto_cut_bot-api:
    <<: *bwrap-security

  auto_cut_bot-cli:
    <<: *bwrap-security
